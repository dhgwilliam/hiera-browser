---
:backends:
  - yaml

:hierarchy:
  - "hostname/%{hostname}"
  - "datacenter/%{::datacenter}"
  - "env/%{::environment}"
  - common

:yaml:
  :datadir: ./spec/fixtures/%{environment}/hieradata

:logger: noop
